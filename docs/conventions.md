# Project conventions

The content below is rendered from [`CLAUDE.md`](https://github.com/rmcd-mscb/gfv2-params/blob/main/CLAUDE.md)
— originally written as Claude-tooling project instructions, but the
contents are general repo conventions every contributor benefits from
(commit hygiene, doc audits, code conventions, the hard-won gotchas that
*will* corrupt outputs if violated). The canonical source stays at the
repo root.

{%
  include-markdown "../CLAUDE.md"
  heading-offset=1
%}
